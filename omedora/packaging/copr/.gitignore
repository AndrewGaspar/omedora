# Locally-built RPMs + repo (rebuild with build-repo.sh; published via COPR later).
output/
repo/
# Generated by build-local.sh / .copr/srpm.sh self-source mode (git archive HEAD).
omedora-self.tar.gz

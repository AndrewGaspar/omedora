#!/bin/bash

# omarchy:summary=Update Omedora-managed packages with dnf (Fedora).
# omarchy:requires-sudo=true

# Compatibility entry point for the pre-Quattro Omedora command name. Keep all
# Fedora package-update policy in the sibling used by omarchy-update-system-pkgs.
exec "$(dirname -- "${BASH_SOURCE[0]}")/fedora/update-system-pkgs" "$@"
